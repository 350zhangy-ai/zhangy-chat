# Core dependencies
pyyaml>=6.0
psutil>=5.9.0

# GUI dependencies
# tkinter is built-in with Python

# Excel export support
openpyxl>=3.0.0

# Build dependencies
pyinstaller>=5.0.0

# Model dependencies (Qwen2.5)
transformers>=4.37.0
accelerate>=0.25.0
torch>=2.0.0
