# Core dependencies
pyyaml>=6.0
psutil>=5.9.0

# GUI dependencies
# tkinter is built-in with Python

# Excel export support
openpyxl>=3.0.0

# Build dependencies
pyinstaller>=5.0.0

# Model dependencies (optional, for MiniMind integration)
torch>=2.0.0
huggingface_hub>=0.20.0
